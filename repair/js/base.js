define(['jquery', 'bootstrap', 'utils/overrides'],
function ($) {  
  /**
   *
   * base entry point loading requirements for base django template and overriding functions as needed by other entry points
   * load this first
   *
   * @author Christoph Franke
   * @module Base
   */
 });