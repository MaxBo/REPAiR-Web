# -*- coding: utf-8 -*-
# Generated by Django 1.11.6 on 2017-11-01 10:58
from __future__ import unicode_literals

from django.db import migrations, models
import django.db.models.deletion


class Migration(migrations.Migration):

    dependencies = [
        ('asmfa', '0004_auto_20171101_1313'),
    ]

    def update_foreign_keys(apps, schema_editor):
        models = {'Actor': (('own_activity', 'activity'), ),
                  'Activity2Activity': (('origin2', 'origin_id'),
                                        ('destination2', 'destination_id'), ),
                  'ActivityStock': (('origin2', 'origin_id'), )
                  }
        for model_name, fields in models.items():
            Model = apps.get_model('asmfa', model_name)
            update_fields = [field[1] for field in fields]
            for row in Model.objects.all():
                for field in fields:
                    new_field = field[1]
                    setattr(row, new_field, getattr(row, field[0]).id)
                row.save(update_fields=update_fields)

    operations = [
        migrations.RunPython(update_foreign_keys, reverse_code=migrations.RunPython.noop),
    ]
